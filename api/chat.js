import fetch from 'node-fetch';

export default async function handler(req, res) {
  // CORS headers
  res.setHeader('Access-Control-Allow-Origin', '*');
  res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type');

  if (req.method === 'OPTIONS') {
    return res.status(200).end();
  }

  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' });
  }

  try {
    const { message } = req.body;

    if (!message) {
      return res.status(400).json({ error: 'Message is required' });
    }

    console.log('Received message:', message);

    // Пробуем использовать DeepSeek API если ключ есть
    const DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;
    let reply;
    let source = 'local';

    if (DEEPSEEK_API_KEY) {
      try {
        const response = await fetch('https://api.deepseek.com/v1/chat/completions', {
          method: 'POST',
          headers: {
            'Authorization': `Bearer ${DEEPSEEK_API_KEY}`,
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            model: 'deepseek-chat',
            messages: [
              {
                role: 'system',
                content: `Ты AI-ассистент сообщества ВКонтакте о вахтовой работе. 
                Отвечай кратко, дружелюбно, веди естественный диалог.
                Основная вакансия: изолировщик промышленного трубопровода.
                Не предлагай варианты в каждом сообщении, веди диалог естественно.
                Отвечай на вопросы по существу.`
              },
              {
                role: 'user',
                content: message
              }
            ],
            max_tokens: 300,
            temperature: 0.8
          })
        });

        if (response.ok) {
          const result = await response.json();
          reply = result.choices[0]?.message?.content;
          source = 'deepseek-api';
          console.log('Successfully used DeepSeek API');
        } else {
          console.log('DeepSeek API failed, using local responses');
          reply = getLocalResponse(message);
        }
      } catch (apiError) {
        console.log('DeepSeek API error, using local responses:', apiError.message);
        reply = getLocalResponse(message);
      }
    } else {
      console.log('No API key, using local responses');
      reply = getLocalResponse(message);
    }

    return res.status(200).json({
      reply: reply,
      success: true,
      source: source
    });

  } catch (error) {
    console.error('Server error:', error);
    return res.status(500).json({ 
      error: 'Internal server error'
    });
  }
}

function getLocalResponse(message) {
  const userMessage = message.toLowerCase().trim();
  
  // Приветствия и начало диалога
  if (userMessage.includes('привет') || userMessage.includes('здравств') || userMessage.includes('hello') || userMessage.includes('hi') || userMessage === 'start') {
    return `Привет! 👋 

Я помощник сообщества «Уфа Работа Вахта Башкирия».

У нас есть вакансия изолировщика промышленного трубопровода с обучением с нуля. 

Рассказать подробнее?`;
  }
  
  // Вакансия - основная информация
  if (userMessage.includes('ваканс') || userMessage.includes('работа') || userMessage.includes('изолиров') || userMessage.includes('чем занима') || userMessage.includes('да') || userMessage.includes('расскаж') || userMessage.includes('подробн')) {
    return `Отлично! 👷‍♂

**Изолировщик промышленного трубопровода** - это:

• Монтаж теплоизоляции на трубопроводах
• Работа с современными материалами  
• Работа в команде на промышленных объектах

💵 **Зарплата:** от 116 550 ₽
📅 **График:** вахта 60 смен
🎓 **Обучение:** с нуля, бесплатно

Что именно вас интересует?`;
  }
  
  // Условия работы
  if (userMessage.includes('услов') || userMessage.includes('проживан') || userMessage.includes('питание') || userMessage.includes('соцпакет') || userMessage.includes('что входит')) {
    return `🏠 **Условия работы:**

• Проживание предоставляем
• 3-разовое питание
• Проезд оплачиваем
• Медкомиссия за наш счет
• Спецодежда и инструменты
• Официальное трудоустройство

Эта вакансия сейчас в тестовом режиме - она актуальна и реальна!`;
  }
  
  // Зарплата
  if (userMessage.includes('зарплат') || userMessage.includes('оплат') || userMessage.includes('доход') || userMessage.includes('зараб') || userMessage.includes('деньг')) {
    return `💵 **Зарплата:**

• От 116 550 рублей в месяц
• Официальная (белая) зарплата
• Премии за хорошую работу
• Выплаты 2 раза в месяц
• Все вовремя и без задержек

Доход растет с опытом!`;
  }
  
  // График работы
  if (userMessage.includes('график') || userMessage.includes('смен') || userMessage.includes('режим') || userMessage.includes('вахт') || userMessage.includes('сколько работа')) {
    return `📅 **График работы:**

• Вахта: 60 рабочих смен
• Можно продлить по желанию
• Стандартный рабочий день
• Отдых между вахтами

Все четко по договору!`;
  }
  
  // Требования
  if (userMessage.includes('требован') || userMessage.includes('нужн') || userMessage.includes('необход') || userMessage.includes('опыт') || userMessage.includes('кто может')) {
    return `📋 **Требования:**

• Готовность работать вахтой
• Ответственное отношение
• Возраст от 18 лет
• Гражданство РФ

🎓 **Опыт НЕ нужен** - всему научим!`;
  }
  
  // Обучение
  if (userMessage.includes('обучен') || userMessage.includes('с нуля') || userMessage.includes('научить') || userMessage.includes('курс') || userMessage.includes('учиться')) {
    return `🎓 **Обучение:**

• Бесплатное обучение 6-7 дней
• Учебный Центр в Мытищах
• Теория + практика
• Все расходы за наш счет
• Затем сразу на объект с зарплатой

Идеально для начала карьеры!`;
  }
  
  // Документы
  if (userMessage.includes('документ') || userMessage.includes('паспорт') || userMessage.includes('снилс') || userMessage.includes('инн') || userMessage.includes('трудовая') || userMessage.includes('что нужно')) {
    return `📄 **Документы:**

• Паспорт РФ
• СНИЛС  
• ИНН
• Трудовая книжка

💊 Медкомиссию проходим бесплатно!`;
  }
  
  // Контакты
  if (userMessage.includes('контакт') || userMessage.includes('телефон') || userMessage.includes('звон') || userMessage.includes('связь') || userMessage.includes('позвонить') || userMessage.includes('как связаться')) {
    return `📞 **Контакты:**

Телефон: 8-960-999-51-14 (Мах)

💬 Или пишите в наше сообщество ВКонтакте!

Для рекрутеров: можем добавить ваши вакансии.`;
  }
  
  // Информация о сообществе
  if (userMessage.includes('сообщество') || userMessage.includes('уфа работ') || userMessage.includes('башкирия') || userMessage.includes('о вас') || userMessage.includes('кто вы')) {
    return `🏢 **Наше сообщество:**

Помогаем с трудоустройством в Уфе и Башкирии. 

💼 Для соискателей: реальные вакансии
👔 Для работодателей: размещение вакансий

Пишите нам — всегда на связи!`;
  }
  
  // Благодарности
  if (userMessage.includes('спас') || userMessage.includes('благодар') || userMessage.includes('thank') || userMessage.includes('пасиб')) {
    return `😊 Рад был помочь! 

Если будут вопросы — обращайтесь!

Удачи в трудоустройстве! 🍀`;
  }
  
  // Прощания
  if (userMessage.includes('пока') || userMessage.includes('до свидан') || userMessage.includes('bye') || userMessage.includes('прощ')) {
    return `👋 До свидания! 

Заходите еще — всегда помогу!

Хорошего дня! 😊`;
  }
  
  // ОБРАБОТКА НЕИЗВЕСТНЫХ ВОПРОСОВ - более естественные ответы
  if (userMessage.includes('как дела') || userMessage.includes('что нового')) {
    return `Всё отлично! 😊 Помогаю людям найти работу.

У нас как раз есть вакансия изолировщика - интересно узнать?`;
  }
  
  if (userMessage.includes('когда') || userMessage.includes('скоро')) {
    return `Вакансия актуальна прямо сейчас! 

Можем начать оформление в ближайшее время.`;
  }

  if (userMessage.includes('где') || userMessage.includes('мест')) {
    return `Работа по всей России вахтовым методом. 

Обучение в Мытищах, затем трудоустройство на объекты.`;
  }

  // Общий fallback
  return `Интересный вопрос! 🤔

Если честно, я лучше всего разбираюсь в вакансии изолировщика и трудоустройстве.

Может, спросите о:
• Условиях работы
• Зарплате и графике  
• Обучении с нуля
• Документах для трудоустройства

Или напишите в наше сообщество — там точно помогут! 💬`;
}
